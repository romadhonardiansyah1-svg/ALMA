[project]
name = "alma"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
    "aiohttp==3.14.3",
    "msgspec==0.21.1",
    "nautilus-trader==1.230.0",
]

[dependency-groups]
dev = [
    "pytest==9.1.1",
    "ruff==0.16.1",
]

[build-system]
requires = ["uv_build==0.12.0"]
build-backend = "uv_build"
