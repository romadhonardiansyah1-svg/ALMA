# ALMA Autonomous Progress

## Current phase
ALMA berjalan continuous di systemd pada Binance Futures TESTNET dan exact MT5 DEMO profile; live arming tetap false. Binance tetap `MANAGE_ONLY`; MT5 tetap `TRADE`. Audit 2026-08-01 memperbaiki Binance book timestamp reorder/resnapshot, recovery semua durable superseded orders, unbounded deterministic execution revisions, durable reversal reduce/open phase, exact MT5 durable-root ownership, and runtime provider-alert health propagation. Current isolated gate: 343 tests across 39 files, zero failures; uv lock, Ruff, format (81 files), compileall, systemd verify, SQLite quick-check, and diff-check pass. Controlled restart leaves `alma.service` and dashboard active with zero restarts/errors and fill count unchanged at five. Broker truth is flat: Binance positions/regular/algo orders `0/0/0`; MT5 connected HEDGING positions/orders `0/0`. Operator switched ALMA to `kr/gpt-5.6-luna-thinking-agentic`. The runtime audit then proved an ALMA setup defect: unconditional per-venue M1 hooks produced 474 accepted and 7 rejected decisions in about 4.5 hours, while repeated provider failures continued generating `NO_DECISION` rows. Runtime now infers only on account change or a newly appearing/changed semantic setup; identical persistent setups are coalesced until they clear. The Luna contract prompt no longer forces every entry to `WAIT_RETEST`: it may select `PASSIVE`, `AGGRESSIVE_LIMIT`, `STOP_ENTRY`, `MARKET_PROTECTED`, `ADAPTIVE`, or `WAIT_RETEST`, with time-sensitive executable scalps directed toward protected immediate modes while price-envelope and mutation guards remain authoritative. A deployed 75-second window produced zero shadow runs, zero router requests, and zero fills, versus the prior two calls per minute. Fresh Luna inference still receives upstream `402 MONTHLY_REQUEST_COUNT`, so runtime and dashboard correctly remain fail-closed (`ok=false`, HTTP 503). Dashboard live state matches the configured MT5 terminal. F6 open-market MT5 field drill, F7 enrichment, F8 host/network/reboot acceptance, Kiro Luna route/account-state reconciliation, and explicit Binance promotion remain outstanding; do not substitute Hermes Sol for ALMA Luna or promote Binance automatically.

## Completed
- Six blueprint documents created and cross-link validated.
- Host/toolchain audited: Python 3.12, uv 0.12.0, 9Router, and NautilusTrader wheel/adaptor smoke test.
- Hermes uses `custom:9router` with `kr/gpt-5.6-sol-thinking-agentic`; ALMA's separate Decision Contract runtime uses `kr/gpt-5.6-luna-thinking-agentic`. OpenAI Codex `gpt-5.6-sol` remains the validated infrastructure fallback for Hermes sessions, while ALMA itself stays fail-closed when Luna is unavailable.
- M0 repository bootstrap completed: initialized local Git on `main`, created an installable Python 3.12 uv project using the native `src/` layout, pinned blueprint-approved runtime/development dependencies, generated `uv.lock`, added standard Python ignores, and added a package import smoke test.
- M1 venue-mode contract completed: added the exact `OFF`, `MONITOR`, `MANAGE_ONLY`, and `TRADE` values plus a shared deterministic gate that permits new exposure only in `TRADE`.
- M1 venue-mode persistence slice completed: added a stdlib SQLite store that enables WAL, persists an explicit venue/mode pair, and restores the typed mode after close/reopen.
- M1 venue-mode mutation completed: bootstrap can initialize a mode only once; later transitions must use the audited shared mutation gate and survive close/reopen.
- M1 reconciliation arithmetic slice completed: added exact `Decimal` execution delta as desired quantity minus actual quantity minus signed pending-order quantity, preserving positive, zero, and negative results.
- M1 idempotency slice completed: added atomic SQLite `request_id` reservation, durable duplicate rejection across restart, and one shared SQLite/WAL connection boundary reused by current stores.
- M1 SQLite foreign-key slice completed: every current store now inherits foreign-key enforcement from the shared database opener.
- M1 `MANAGE_ONLY` quantity-guard slice completed: a shared exact-`Decimal` gate permits only strict, sign-preserving exposure reductions and rejects unchanged quantity, increases, and reversals.
- M1 FR-031 cross-mode quantity-guard slice completed: `TRADE` permits changed target quantities, `OFF` and `MONITOR` reject them, and unchanged targets remain rejected because they require no order.
- M1 Decision Contract top-level strictness slice completed: added the documented v1 top-level shape and a shared `msgspec` JSON parser that rejects unknown top-level fields.
- M1 Decision Contract nested strictness and first enum slices completed: verified the existing strict `target` and `entry` objects, replaced permissive target-item dictionaries with the documented strict `price`/`close_fraction` shape, and constrained `action`, `entry.mode`, and `on_missed` to their documented values.
- M1 Decision Contract remaining entry enum and target-allocation slices completed: constrained `on_partial_fill` to its three documented values and reject exact-decimal target fractions whose aggregate exceeds one.
- M1 Decision Contract version/timestamp slices completed: the v1 decoder rejects policy versions other than `alma-v1`, uses msgspec's native RFC3339 datetime decoding, and rejects non-UTC `created_at` offsets.
- M1 Decision Contract runtime-context slices completed: explicit validators reject snapshot-ID mismatches and contracts observed after `created_at + ttl_seconds`, with current state/time supplied by the caller.
- M1 technical increment-guard slice completed: one exact-`Decimal` guard validates caller-supplied volume-step/tick-size increments and fails closed for misaligned values plus non-positive or non-finite operands.
- M1 technical range-guard slice completed: one exact-`Decimal` guard validates finite values against finite caller-supplied inclusive bounds and fails closed for outside, inverted, or non-finite inputs.
- M1 technical stop-distance guard completed: one direction-agnostic exact-`Decimal` guard validates that two finite prices meet finite, non-negative caller-supplied minimum-distance metadata.
- M1 technical margin guard completed: one exact-`Decimal` guard accepts finite, non-negative caller-supplied margin only when available margin meets or exceeds required margin.
- M1 venue quantity fail-closed slice completed: non-finite actual or desired quantities are rejected in every venue mode without Decimal comparison exceptions.
- M1 target-allocation finite slice completed: quiet/signaling NaN and infinity target fractions are rejected before aggregation.
- M1 Decision Contract decimal-string hardening completed: malformed and non-finite values are rejected at the shared parser boundary for volume, entry prices, invalidation price, target prices, uncertainty, and target fractions, while the contract continues to retain exact decimal values as strings.
- M1 AI repair/fallback coordinator slices completed: malformed or schema-invalid Decision Contract output gets at most one repair attempt; deterministic semantic validation failures do not trigger repair; timeout, connection failure, and normalized rate-limit/quota/5xx failures advance through a bounded provider iterable using the identical request object; exhaustion returns no new decision.
- M1 FR-013 retry-policy slices completed: the primary provider gets exactly one retry for transient timeout/connection failures before fallback, every attempt receives the identical request object, and provider-unavailable signals such as quota/rate-limit exhaustion advance immediately without a predictably doomed retry.
- M2 public USDⓈ-M node-configuration tracer completed: ALMA now wires one native Nautilus Binance USDT-futures LIVE data client into `TradingNodeConfig`, with no credentials and no execution clients.
- M2 native Binance data-factory registration completed: ALMA constructs an unbuilt `TradingNode`, registers only `BinanceLiveDataClientFactory`, and exposes no execution factory or client.
- M2 public connectivity smoke slices completed: added a credential-free native-node harness whose connection wait has an explicit timeout, whose startup failures retain their original exception, and whose node is stopped and disposed on timeout, success, or failure; a live public USDⓈ-M run connected successfully without credentials or execution clients.
- M2 BTCUSDT public trade tracer completed: added one native Nautilus strategy that subscribes to `BTCUSDT-PERP.BINANCE`, keeps the first trade tick, and runs through a bounded smoke harness with deterministic startup-failure precedence and guaranteed stop/dispose; a live credential-free run received a BTCUSDT trade tick without an execution client.
- M2 order-book tracer and raw sequence contract completed: native Nautilus subscription received both BTCUSDT snapshot and delta batches; a separate deterministic Binance Futures `U/u/pu` state validates snapshot bridging, stale events, contiguous previous-final chaining, gap invalidation, and recovery only after a new snapshot.
- M2 incremental bars completed: exact-Decimal trade events build UTC-aligned M1 OHLCV and closed M1 bars derive M5/M15/H1 without historical redownload or synthetic empty bars; invalid values and out-of-order time fail closed.
- M1 final exit gate completed: added schema-versioned append-only decisions/intents/order/fill/audit records, `BEGIN IMMEDIATE` read-validate-write transactions, atomic partial/final fill lifecycle, consistent SQLite backup, 5-second busy timeout, episode reconstruction, trusted-clock freshness, accepted-decision enforcement, broker-authoritative restart/resync, explicit MANAGE/FREEZE/CLOSE_AND_DISABLE plans, and durable close-and-disable completion after fresh venue truth confirms zero position and pending exposure. Direct intent insertion and unaudited mode overwrite APIs were removed.
- M2/F2 public state implementation completed: native trade/quote/mark/funding events feed one exact-Decimal incremental state; raw Binance `U/u/pu` is validated before Nautilus drops `U/pu`; gap invalidation requests the native snapshot rebuild; compact immutable snapshots include deterministic state ID, observed time/age, M1/M5/M15/H1 bars, spread, velocity, volatility, flow imbalance, and feed validity.
- M2/F2 persistence and observability completed: live and replay share the same `MarketEvent.apply()` path; bounded partitioned Parquet recording preserves session/sequence order; deterministic replay verifies state hashes; read-only health/state/SSE endpoints expose event/gap/reconnect/age/p95/disk metrics; no mutation endpoint, execution client, credential, scheduler, or cron was added.
- F3 deterministic feature state completed: closed bars only; H1 return/volatility/regime, M15 liquidity/position/displacement/acceptance, M5 compression, and M1/tick spread/velocity/flow/top-book values are exact-Decimal and hashed. Future bars and closed-bar revisions fail closed.
- F3 two-strategy boundary completed: Liquidity Sweep Reversal and Liquidity Vacuum Continuation return immutable evidence/invalidation context only. They contain no quantity, order, submit, sizing, or execution path and reject invalid depth, insufficient confirmation, adverse flow/book state, excessive funding, and movement that does not exceed modeled cost.
- F3 realistic research replay completed: recorded ordering, event-time watermark, and candidate event indices prevent same-event/future fills; executable bid/ask, fees, discrete settlement funding, latency, adverse slippage, partial fills, missed entries, overlap, disconnect/end-of-data mark-to-market, single-pass purged OOS metrics, and deterministic hashes are covered.
- F4 shadow decision service completed: compact canonical snapshots have recursive secret-key rejection and a 64 KB cap; hook coalescing excludes ticks; loopback transport has timeout/token caps; identical-request fallback is attempt/deadline bounded with aggregate telemetry; only decode/schema failures get one repair; semantic failures and exhaustion yield no target. Append-only runs, hypothetical reconciliation, ledger-derived outcome attribution, decayed calibration, and immutable `SHADOW` provenance are implemented. The shared mutation gate accepts only `EXECUTION`, so shadow decisions cannot create intents.
- F5 deterministic execution core completed locally: exact metadata/margin/freshness guards, desired-state reread immediately before every submit/reprice, atomic winner-only order reservation, deterministic client-order IDs, async submit/cancel semantics, timeout/restart recovery without resubmit, TTL and partial-fill policies, protection verification, emergency flatten, and durable emergency-stop mode handling are implemented.
- F5 native Binance Testnet bridge completed locally: one Nautilus `Strategy` uses only native cache/events and native execution commands, records fills idempotently with fee currency and adverse slippage, creates deterministic reduce-only stop/target children per fill, maps market-protected entry to bounded LIMIT IOC, and attaches to the native Testnet node. No second REST/WebSocket client or credential was added.
- F7 local implementation completed: a loopback-only owner-authenticated aiohttp dashboard provides health, venue-separated truth/orders/fills, exact-Decimal fee/funding summaries, shadow/fallback telemetry, append-only calendar revisions with deterministic release phases, and sub-two-second SSE. Mode controls require exact confirmation and call the shared `MutationGate`; standalone controls fail closed. Recursive secret-like keys are rejected before rendering and cross-venue money is deliberately not aggregated.
- F8 hardening is active: hardened native systemd units cover the runtime, dashboard, MT5, continuous MT5 monitor, daily verified backup, and health checks. Health now requires fresh runtime status and the configured MT5 terminal identity. The operations CLI provides atomic SQLite backup/clean restore, `quick_check`, Parquet SHA-256 manifest verification, symlink-safe retention, and NTP/disk/database/rejection/fallback/MT5 alerts.

## Validation evidence
- Blueprint validator: 6 documents, 1,010 lines, no broken relative links or missing required sections.
- `nautilus_trader==1.230.0` import and Binance adapter import succeeded in a temporary Python 3.12 environment.
- Direct inference through the active 9Router model returned `ROUTER_OK`.
- 2026-07-31 M0 bootstrap: `uv lock --check` resolved 33 packages; `uv run pytest -q` passed 1 test; `uv run ruff check .` passed; `uv run ruff format --check .` reported 10 files already formatted; `git diff --check` passed.
- 2026-07-31 M1 venue modes: both RED tests failed for the expected missing contracts before implementation; `uv lock --check` resolved 33 packages; `uv run pytest -q` passed 3 tests; `uv run ruff check .` passed; `uv run ruff format --check .` reported 12 files already formatted; `git diff --check` passed; an independent `/tmp/hermes-verify-*` script confirmed the exact values and `[False, False, False, True]` exposure-gate result and was removed after execution.
- 2026-07-31 M1 venue-mode persistence: both RED tests failed for the expected missing store/read-write contracts before implementation; `uv lock --check` resolved 33 packages; `uv run pytest -q` passed 5 tests; `uv run ruff check .` passed; `uv run ruff format --check .` reported 14 files already formatted; `git diff --check` passed; an independent `/tmp/hermes-verify-*` script confirmed WAL and close/reopen recovery and was removed after execution.
- 2026-07-31 M1 venue-mode update: the RED test failed with the expected SQLite unique-key conflict; the minimal `ON CONFLICT` update made it green; `uv lock --check` resolved 33 packages; `uv run pytest -q` passed 6 tests; `uv run ruff check .` passed; `uv run ruff format --check .` reported 14 files already formatted; `git diff --check` passed; an independent `/tmp/hermes-verify-*` script confirmed `MONITOR` → `TRADE` restart recovery and was removed after execution.
- 2026-07-31 M1 reconciliation arithmetic: three vertical RED-GREEN cycles covered positive, zero, and negative results; `uv lock --check` resolved 33 packages; `uv run pytest -q` passed 9 tests; `uv run ruff check .` passed; `uv run ruff format --check .` reported 16 files already formatted; `git diff --check` passed; an independent `/tmp/hermes-verify-*` script confirmed all three exact `Decimal` results and was removed after execution.
- 2026-07-31 M1 idempotency: the RED test failed for the expected missing idempotency contract; atomic `INSERT OR IGNORE` made first reservation succeed and the same ID fail after reopen; cleanup extracted the duplicated SQLite/WAL opener; `uv lock --check` resolved 33 packages; `uv run pytest -q` passed 10 tests; `uv run ruff check .` passed; `uv run ruff format --check .` reported 19 files already formatted; `git diff --check` passed; an independent `/tmp/hermes-verify-*` script confirmed durable duplicate rejection and was removed after execution.
- 2026-07-31 M1 SQLite foreign keys: the RED test failed with the expected `(0,)` result before implementation; one `PRAGMA foreign_keys=ON` in the shared opener made it green for all current stores; `uv lock --check` resolved 33 packages; `uv run pytest -q` passed 11 tests; `uv run ruff check .` passed; `uv run ruff format --check .` reported 20 files already formatted; `git diff --check` passed. Ad-hoc `/tmp/hermes-verify-alma-foreign-keys.py` verification used a `TemporaryDirectory`, confirmed `(1,)`, and confirmed an orphan insert raises `sqlite3.IntegrityError`; the script was removed after passing. The simplify pass found no material issue in this change; one unrelated test-order suggestion was deliberately left outside scope.
- 2026-07-31 M1 `MANAGE_ONLY` quantity guard: the first RED test failed on the expected missing shared quantity-change contract; its minimal magnitude rule passed reduction/unchanged/increase coverage. A second RED test then exposed that magnitude alone admitted a smaller opposite-side reversal; requiring sign preservation made both tracer tests green. `uv lock --check` resolved 33 packages; `uv run pytest tests/test_venue_modes.py -v` passed 4 tests; `uv run pytest -q` passed 13 tests; `uv run ruff check .` passed; `uv run ruff format --check .` reported 20 files already formatted; `git diff --check` passed. Ad-hoc `/tmp/hermes-verify-alma-manage-only-*.py` verification passed six exact cases (long/short reduction, close-to-zero, unchanged, increase, and reversal), then the script was removed. The required reuse, quality, efficiency, and altitude cleanup review ran inline because the configured delegation cap is three and found no material cleanup.
- 2026-07-31 M1 FR-031 cross-mode quantity guard: the RED behavior-matrix test failed exactly because `TRADE` returned false for a changed target; one shared predicate change made `TRADE` accept changed quantities while preserving `OFF`, `MONITOR`, `MANAGE_ONLY`, and unchanged-target behavior. `uv lock --check` resolved 33 packages; `uv run pytest tests/test_venue_modes.py -v` passed 5 tests; `uv run pytest -q` passed 14 tests; `uv run ruff check .` passed; `uv run ruff format --check .` reported 20 files already formatted; changed-file whitespace checks passed. Ad-hoc `/tmp/hermes-verify-*.py` verification confirmed the exact cross-mode result `{OFF: (False, False), MONITOR: (False, False), MANAGE_ONLY: (False, False), TRADE: (True, False)}`, the `MANAGE_ONLY` reduction/reversal boundary, and removed the temporary script after passing. The required reuse, quality, efficiency, and altitude cleanup review ran inline because the configured delegation cap is three and found no material cleanup.
- 2026-07-31 M1 Decision Contract top-level strictness: the RED test failed for the expected missing `alma.decision_contract` module; the minimal strict `msgspec.Struct` parser then rejected the extra top-level field, with the assertion corrected to match `msgspec`'s actual lowercase diagnostic. Declared the already-locked `msgspec==0.21.1` transitive package as a direct dependency because production code imports it, then regenerated lock metadata without changing the 33-package resolution. `uv lock --check` resolved 33 packages; `uv run pytest tests/test_decision_contract.py::test_rejects_unknown_top_level_field -v` passed 1 test; `uv run pytest -q` passed 15 tests; `uv run ruff check .` passed; `uv run ruff format --check .` reported 22 files already formatted; `git diff --check` passed. An independent `/tmp/hermes-verify-alma-decision-contract-*.py` script decoded a valid contract, rejected an unknown top-level field with `msgspec.ValidationError`, printed `AD_HOC_OK`, and was removed after passing. Reuse, quality, and efficiency reviewers ran concurrently and the altitude review ran inline due the three-agent delegation cap; all four found no material cleanup.
- 2026-07-31 M1 Decision Contract nested/enums: the already-present strict `target` and `entry` unknown-field tests were verified green rather than misreported as new RED cycles. Four vertical RED-GREEN cycles then proved and fixed permissive target-item dictionaries, free-form `action`, free-form `entry.mode`, and free-form `on_missed`; each RED failed because invalid input was accepted, and each minimal typed schema/`StrEnum` change made its targeted test and Decision Contract suite green. The simplify pass ran reuse, quality, and efficiency reviewers concurrently plus an inline altitude review; it removed the duplicated full-contract fixture from the top-level unknown-field test. The enum exact-value test suggestion was deferred because each accepted enum value has not yet been introduced through its own behavior slice, and `on_partial_fill` was retained as the next TDD task rather than bundled into cleanup. Final validation: `uv run pytest -q` passed 21 tests in 0.25s; `uv run ruff check .` passed; `uv run ruff format --check .` reported 22 files already formatted; `git diff --check` passed.
- 2026-07-31 M1 Decision Contract partial-fill/allocation: two vertical RED-GREEN cycles rejected an undocumented `on_partial_fill` value and a target allocation totaling `1.01`. The minimal implementation added the documented `KEEP_REMAINDER`, `REPRICE_REMAINDER`, and `CANCEL_REMAINDER` enum and one exact `Decimal` aggregate check at the shared parser boundary; the valid fixture continues to prove that equality at `1.00` is accepted. No undocumented positivity, per-item range, precision, or error-normalization policy was invented. Reuse, quality, and efficiency reviewers ran concurrently and an inline altitude review found no code cleanup or misplaced validation; the only material review note was to refresh this durable record. Final validation: `uv run pytest -q` passed 23 tests in 0.85s; `uv run ruff check .` passed; `uv run ruff format --check .` reported 22 files already formatted; `git diff --check` passed.
- 2026-07-31 M1 Decision Contract version/timestamp: two vertical RED-GREEN cycles rejected `alma-v2` at the v1 decoding boundary and rejected a valid RFC3339 timestamp carrying a non-zero offset. The minimal implementation uses `Literal["alma-v1"]`, msgspec's native `datetime` conversion, and a zero-offset check; no custom timestamp parser was added. The first GREEN run exposed only msgspec's actual `Literal` diagnostic (`Invalid enum value`), so the test assertion was aligned with observed behavior before passing. Reuse, quality, and efficiency reviewers ran concurrently and the inline altitude review found no material cleanup. Final validation: `uv run pytest -q` passed 25 tests in 0.29s; `uv run ruff check .` passed; `uv run ruff format --check .` reported 22 files already formatted; `git diff --check` passed.
- 2026-07-31 M1 Decision Contract runtime validation: two vertical RED-GREEN cycles introduced explicit context-aware boundaries for expected snapshot identity and deterministic expiry. The RED runs failed at collection for the expected missing validator functions; minimal implementations reject a mismatched `state_id` and reject only `now > created_at + ttl_seconds`, leaving exact-deadline behavior unclaimed. The JSON decoder remains context-free. Reuse, quality, and efficiency reviewers ran concurrently; the inline altitude review confirmed the checks belong at the shared runtime boundary. Safe cleanup consolidated repetitive enum/literal and unknown-field tests into parameterized cases with descriptive IDs. Final validation: `uv run pytest tests/test_decision_contract.py -v` passed 13 cases; `uv run pytest -q` passed 27 tests in 0.18s; `uv run ruff check .` passed; `uv run ruff format --check .` reported 22 files already formatted; `git diff --check` passed.
- 2026-07-31 M1 technical increment guard: RED first proved the shared guard was missing; the initial misalignment and invalid-metadata tests supported only rejection, so implementation was deliberately reduced to `False` before an aligned-value RED test proved the positive behavior. The final exact-`Decimal` predicate accepts aligned values, rejects misalignment, fails closed for non-positive increments, and—after the simplify review exposed the external-input edge—rejects non-finite values/increments without raising. Reuse, quality, and efficiency reviewers ran concurrently; no reuse or quality cleanup was needed and the finite-operand finding was fixed through its own RED-GREEN cycle. Final validation: `uv run pytest tests/test_technical_guards.py -v` passed 4 tests; `uv run pytest -q` passed 31 tests in 0.53s; `uv run ruff check .` passed; `uv run ruff format --check .` reported 24 files already formatted; `git diff --check` passed.
- 2026-07-31 M1 technical range guard: four vertical RED-GREEN steps introduced the missing guard, rejected values below/above bounds, changed strict interior comparison to documented inclusive endpoints, and failed closed for inverted/non-finite operands. The implementation uses only exact `Decimal` finite checks and `minimum <= value <= maximum`; venue-specific limits remain caller-owned. Reuse, quality, and efficiency reviewers ran concurrently and the inline altitude review found no material cleanup. Final validation: `uv run pytest tests/test_technical_guards.py -v` passed 8 tests; `uv run pytest -q` passed 35 tests in 0.23s; `uv run ruff check .` passed; `uv run ruff format --check .` reported 24 files already formatted; `git diff --check` passed.
- 2026-07-31 M1 technical stop-distance guard: four vertical RED-GREEN steps introduced the missing guard, rejected below-minimum distance, changed strict comparison to inclusive threshold, and failed closed for negative/non-finite distance metadata or non-finite prices. The implementation remains direction-agnostic and uses `abs(first - second)`; trade-side and venue-specific stop policy remain caller-owned. Reuse, quality, and efficiency reviewers ran concurrently and the inline altitude review found no material cleanup. Final validation: `uv run pytest tests/test_technical_guards.py -v` passed 12 tests; `uv run pytest -q` passed 39 tests in 0.80s; `uv run ruff check .` passed; `uv run ruff format --check .` reported 24 files already formatted; `git diff --check` passed.
- 2026-07-31 M1 margin/finite-input guards: verified the already-present margin slice with above, below, equality, negative, NaN, and infinity cases. Two fresh RED-GREEN cycles then reproduced a `decimal.InvalidOperation` in `MANAGE_ONLY` for non-finite quantities and acceptance/unsafe aggregation of non-finite target fractions; minimal finite checks now fail closed. The simplify pass ran reuse, quality, and efficiency reviewers concurrently plus an inline altitude review. Its signaling-NaN finding received its own RED-GREEN regression cycle and moved fraction validation before arithmetic. No abstraction was added and caller-supplied venue metadata remains outside these predicates. Final validation: `uv lock --check` resolved 33 packages; `uv run pytest -q` passed 47 tests in 0.28s; `uv run ruff check .` passed; `uv run ruff format --check .` reported 24 files already formatted; `git diff --check` and changed-file no-index whitespace checks passed.
- 2026-07-31 M1 Decision Contract decimal strings: three vertical RED-GREEN cycles first rejected malformed strings across all seven remaining numeric field locations, then rejected `NaN`, `sNaN`, and `Infinity` across those locations, and finally normalized malformed target fractions that previously leaked `decimal.InvalidOperation`. The implementation performs one guarded exact-`Decimal` conversion at the shared parser boundary and retains the existing string-valued contract model; no positivity, ordering, range, precision, side/action consistency, or safety-buffer policy was invented. Reuse, quality, and efficiency reviewers ran concurrently and altitude was checked inline. Safe cleanup replaced duplicated value-only test inventories with one explicit field-fragment table; an unbounded-target micro-optimization was deferred because no target-count contract or production hot path exists. RED evidence was 7 expected non-raises, 21 expected non-raises, and one mismatched `InvalidOperation`; targeted GREEN suites reached 45 Decision Contract cases. Final validation: `uv run pytest -q` passed 76 tests in 0.31s; `uv run ruff check .` passed; `uv run ruff format --check .` reported 24 files already formatted; `git diff --check` passed.
- 2026-07-31 M1 AI repair/fallback coordinators: four adjacent TDD slices added one bounded invalid-output repair, timeout fallback with identical request identity, connection-error fallback, and a shared `ProviderUnavailable` signal for adapter-normalized rate-limit/quota/5xx failures. RED runs failed for the expected missing coordinator/module/signal or propagated connection failure. The simplify review then exposed that broad `ValueError` handling repaired deterministic invalid-decimal contracts; a fresh RED test proved the repair callback was incorrectly invoked, and narrowing to `msgspec.DecodeError` preserved malformed/schema-invalid repair while leaving semantic failures untouched. Safe cleanup adopted Python 3.12 type-parameter syntax required by Ruff. Reuse, quality, and efficiency reviewers ran concurrently; altitude was reviewed inline and found the parser/fallback boundaries correctly separated. The documented one transient retry remains a separate behavior slice; telemetry remains deferred with its unspecified audit-event contract. Final validation: `uv lock --check` resolved 33 packages; `uv run pytest -q` passed 81 tests in 0.25s; `uv run ruff check .` passed; `uv run ruff format --check .` reported 26 files already formatted; `git diff --check` passed.
- 2026-07-31 M1 FR-013 retry policy: the first RED test proved a primary timeout advanced directly to fallback; the minimal loop now retries only the first provider once and preserves request identity. The existing exhaustion test was aligned with the documented primary/primary/fallback sequence and the targeted suite passed. Reuse, quality, and efficiency reviewers ran concurrently and altitude was checked inline. Review found no safe cleanup but identified that the existing `ProviderUnavailable` signal includes non-transient quota/rate-limit exhaustion; a second RED test proved it was retried twice, and narrowing the exception branches made provider unavailability advance immediately while timeout/connection failures retain one primary retry. Async provider callbacks, aiohttp exception normalization, retryable-5xx classification, cooldowns, and telemetry remain adapter-level work rather than speculative generic abstractions. Final validation: `uv lock --check` resolved 33 packages; `uv run pytest -q` passed 82 tests in 0.28s; `uv run ruff check .` passed; `uv run ruff format --check .` reported 26 files already formatted; `git diff --check` plus no-index checks for the untracked changed files passed.
- 2026-07-31 M2 public USDⓈ-M node configuration: the initial RED test proved the ALMA Binance configuration boundary was missing. A first GREEN implementation merely wrapped `BinanceDataClientConfig`; reuse and quality reviewers correctly identified that as an unused pass-through abstraction, so it was discarded. Runtime inspection of pinned NautilusTrader 1.230.0 established the real `TradingNodeConfig.data_clients` seam, and a fresh RED test failed for the missing node-level function. The replacement wires a native `BinanceDataClientConfig` under `BINANCE` with `USDT_FUTURES` and `LIVE`, retains `None` credentials, and leaves `exec_clients` empty. No symbols, private streams, execution factory, credentials, or network access were added. Reuse, quality, and efficiency reviewers ran concurrently and altitude was checked inline; the only material cleanup was deepening the tracer from constructor wrapper to node wiring. Final validation: `uv lock --check` resolved 33 packages; `uv run pytest -q` passed 83 tests in 1.79s; `uv run ruff check .` passed; `uv run ruff format --check .` reported 28 files already formatted; `git diff --check` plus no-index checks for changed untracked files passed.
- 2026-07-31 M2 native factory registration: a local RED test failed for the missing `public_usdm_node` constructor. The minimal implementation creates the existing credential-free USDⓈ-M node config and registers only `BinanceLiveDataClientFactory`; it deliberately does not call `build`, connect, run, or register an execution factory. The test uses the public duplicate-registration `KeyError` contract rather than private Nautilus state, disposes the node, and supplies a local event loop to avoid Python 3.12's ambient-loop deprecation warning. Final validation: `uv lock --check` resolved 33 packages; `uv run pytest -q` passed 84 tests in 1.93s with no warnings; `uv run ruff check .` passed; `uv run ruff format --check .` reported 28 files already formatted; `git diff --check` plus no-index checks for all changed untracked files passed.
- 2026-07-31 M2 public connectivity smoke: four vertical RED-GREEN cycles added a connection-wait timeout with guaranteed stop/dispose, surfaced startup failures immediately instead of misreporting them as connection timeouts, preserved startup `TimeoutError` provenance, and made an already-completed startup failure win over a same-turn connected state. A 10 ms readiness poll avoids a hot zero-delay loop. The live credential-free harness built the native Binance USDⓈ-M data client, connected to `https://fapi.binance.com` / `wss://fstream.binance.com/market`, returned `PUBLIC_USDM_CONNECTED=True`, then stopped and disposed every engine; no execution client, private stream, subscription, or credential was used. Reuse, quality, and efficiency reviewers ran concurrently and altitude was checked inline; review-driven correctness findings received their own RED-GREEN regressions. Test-fake consolidation was deferred because it would add test abstraction without reducing production risk. Final validation: `uv lock --check` resolved 33 packages; `uv run pytest -q` passed 88 tests in 1.85s; `uv run ruff check .` passed; `uv run ruff format --check .` reported 28 files already formatted; `git diff --check` passed.
- 2026-07-31 implementation-plan/trade-tracer batch: added `IMPLEMENTATION_PLAN.md` with F0–F9 work packages, evidence gates, critical path, and operator-controlled boundaries; aligned `AGENTS.md` to interactive development with no cron scheduling. Added native `FirstTradeTick`, node registration, bounded first-event smoke lifecycle, and a regression ensuring startup failure wins over an already-visible tick. A live public run returned `ALMA_TRADE_SMOKE=OK instrument=BTCUSDT-PERP.BINANCE price=63882.80`; no credential, private stream, or execution client was used. Final validation: `uv lock --check` resolved 33 packages; `uv run pytest -q` passed 94 tests in 1.94s; `uv run ruff check .` passed; `uv run ruff format --check .` reported 29 files already formatted; `git diff --check` passed; plan/blueprint validator reported 10 phases and 6 blueprints; cron list remained empty.
- 2026-07-31 M2 order-book batch: added the minimal raw Binance Futures sequence state for `U/u/pu` and a native Nautilus `FirstBookStream` tracer with bounded lifecycle. The live credential-free run returned `ALMA_BOOK_SMOKE=OK instrument=BTCUSDT-PERP.BINANCE snapshot_sequence=11179381862053 delta_sequence=11179381866285 snapshot_items=2001 delta_items=15`. The large valid downstream sequence jump proves `lastUpdateId + 1` is not a safe rule. Nautilus 1.230.0 retains only final `u` in `OrderBookDeltas`, so accurate gap detection must be wired before that lossy conversion; this is not falsely claimed complete. Final validation: `uv lock --check` resolved 33 packages; `uv run pytest -q` passed 104 tests in 1.89s; Ruff, format, and diff checks passed; cron remained empty.
- 2026-07-31 M2 incremental bars: added one deterministic state module for M1 OHLCV from exact-Decimal trades and M5/M15/H1 derivation only from closed M1 bars. Tests cover UTC bucket boundaries, sparse minutes without synthetic bars, positive finite input, exact OHLCV, and monotonic event time. Nautilus remains the live subscription/timing engine; ALMA did not add a second framework. Final validation: `uv lock --check` resolved 33 packages; `uv run pytest -q` passed 109 tests in 2.07s; Ruff, format, and diff checks passed.
- 2026-07-31 M1 finalization: implemented the minimum durable ledger and shared mutation/recovery boundary required by the F1 plan. Failure tests cover duplicate requests across restart, missing/stale/divergent venue truth, signed pending and partial fills, terminal order transitions, append-only enforcement, rejected decisions, transaction rollback when audit/fill writes fail, mode-transition rollback, backup/reopen reconstruction, close-and-disable restart completion, and SQL trace proof that `BEGIN IMMEDIATE` precedes mode/order reads. Decision Contract validation rejects invalid target side, non-positive TTL/prices, negative volume/fractions, reversed entry envelopes, and uncertainty outside `[0,1]`. Final validation: full suite passed 138 tests in 2.93s; lock, Ruff, format, diff, F1 artifact, and zero-cron checks passed.
- 2026-07-31 M2/F2 implementation validation: full suite passed 163 tests; `uv lock --check`, Ruff, format, and `git diff --check` passed. Live guarded-state smoke populated quote/trade/mark/funding, recovered depth after a raw sequence gap, produced a snapshot in 0.130 ms, and used no credentials or execution client. CLI short soak processed 707 events, ended with valid depth, replayed the same state hash, and measured p95 event-to-state latency of 0.043 ms. The short run explicitly reported `full_24h_gate=false`.
- 2026-07-31 F3/F4 final validation: full suite passed 215 tests; Ruff and compile checks passed. Reviewer regressions cover cross-fold position overlap, discrete funding settlement from the native Binance mark stream, cross-stream exit watermark, stale detector rejection, prospective vacuum edge, secret/oversized snapshots, semantic action/target coherence, idempotent shadow requests, fallback deadline/telemetry, ledger-derived outcome attribution, and shadow-provenance rejection at the shared mutation gate. No real provider, credential, execution client, or order submission was used.
- 2026-07-31 F5 local validation: targeted execution/native-bridge/ledger suite passed 36 tests; secret-boundary/configuration/read-only-smoke/evidence tests passed 10 tests; full suite passed 248 tests. `uv lock --check`, Ruff, format, compile, and diff checks passed. Native Testnet runtime attachment was exercised without build/connect; no private connection or order submission was used. A fail-closed stdlib INI loader/hidden-input installer targets `/root/.config/alma/binance-testnet.ini`; the file is owner-only `0600`, ignored by Git, and intentionally empty until a rotated key is entered directly on the VPS. A read-only native smoke and private atomic evidence CLI are ready to prove Testnet data/execution connectivity, instrument metadata, account identity, balances, positions, and orders without submitting or canceling orders.

## Blockers
- 9Router is reachable and its health endpoint is green, but its only configured provider connection is Kiro and upstream returns HTTP 402 `MONTHLY_REQUEST_COUNT` (`You have reached the limit`) — as of 2026-08-08 the provider has no active credentials at all (`No active credentials for provider: kiro`). Hermes is configured for Sol thinking-agentic and ALMA separately for Luna thinking-agentic; ALMA authenticates to 9Router with a dedicated owner-only bearer-key environment file. Hermes CLI can still answer through its Codex fallback, but that is not proof of 9Router success. ALMA correctly records `NO_DECISION` and production health reports `FALLBACK_EXHAUSTED_REPEATED`. On 2026-08-08 operator explicitly approved switching ALMA to `stepfun/step-3.7-flash` (verified 200 OK); the alert will clear once three consecutive shadow runs succeed against the new model. The blocker on `kr/*` credentials remains; code must not suppress the alert.
- F2 closure is time-gated by `alma-f2-soak-20260731T1532Z.service`, started 2026-07-31 15:32:26 UTC. Do not combine its interrupted predecessor or mark F2 complete until its final evidence proves full duration, valid depth, replay-hash equality, and p95 latency at or below 50 ms.
- F3 baseline promotion remains `INSUFFICIENT_SAMPLE`: the interim 230,000-event replay produced zero candidates, so profitability and risk statistics are not established.
- F5 integrated Testnet execution is proven end-to-end: accepted candidate, native submit, broker fill, venue-resident reduce-only SL/TP, restart reconciliation, owned emergency market flatten, automatic protection cancellation, and zero post-drill position/regular/algo orders. Both zero-exposure soak attempts are superseded: the first stopped early, while the replacement correctly stopped when authorized continuous execution opened a protected position. A future F5 soak must be execution-aware rather than treating every intended position as failure.
- F6 startup/restart and fresh broker truth are proven for login `[REDACTED]`, exact server `Exness-MT5Trial6`, `DEMO`, `AUTO → HEDGING`, and `XAUUSD`. File IPC removed the WebRequest permission dependency. The open-market minimum-fill, venue-resident SL/TP, partial-close/reversal, foreign-exposure, and owned emergency-flatten field drill remains pending and every drill must end at zero position/order/command. Unit `alma-f6-soak-20260801T0604Z.service` is only a pre-drill readiness monitor; formal seven-day acceptance must restart from zero after the open-market field drill passes.
- F7 live calendar/DXY/yield/OI/liquidation enrichment and reaction semantics still require operator-selected providers. The dashboard is enabled only on `127.0.0.1:8080`, returns `401` without its owner-only bearer token, and remains suitable for SSH local forwarding.
- F8 now has enabled `alma.service`, private dashboard, minute health timer, verified daily backup timer, MT5 bridge, and MT5 terminal services. Production acceptance still requires external alert delivery, chosen risk/drawdown thresholds, and host/network/venue/full-reboot drills. A controlled core-only restart has passed. 9Router 0.5.45 must not be multi-instanced.
- Runtime profiles now support configurable Binance `LIVE|TESTNET|DEMO` and MT5 `DEMO|REAL`, custom instrument/server/symbol/terminal identity, separate credentials, exact account identity pinning, and explicit `ALMA_LIVE_APPROVED=true` arming without source edits. The active deployment remains Testnet/DEMO. Environment selection never auto-promotes a persisted mode to `TRADE`; promotion remains a separate audited `MutationGate` transition.
- Real-money activation still requires explicit operator approval because it changes actual money behavior; this is a configurable arming boundary, not a permanent capability ban or runtime duration limit.

## Next smallest task
Keep the configured Testnet/DEMO supervisor and continuous monitors running. When the MT5 market is open, run the existing venue adapter through minimum fill, venue-resident protection, HEDGING partial close/reversal, foreign-exposure rejection, restart reconciliation, and owned emergency flatten; every controlled drill must end with zero position/order/command. Runtime operation itself is indefinite; finite soak durations are optional acceptance windows only.

## Run log
- 2026-08-08: Removed duplicate user-level systemd deployments of ALMA. Operator chose to keep the system-level deployment (`/etc/systemd/system/`) as the single source of truth. Stopped and disabled user-unit duplicates under `/root/.config/systemd/user/` (alma, alma-mt5-bridge, alma-mt5-wine, alma-mt5-monitor, alma-dashboard, alma-health), moved their unit files to `/root/.config/systemd/user-backup-20260808/`, and ran `systemctl --user daemon-reload`. Killed zombie PIDs that were holding port 8765 and stale `terminal64.exe` instances from the duplicate stack. Cleared stale MT5 state in SQLite (`/root/alma/var/alma.db`, backup at `/root/alma/var/alma.db.bak-pre-mt5-reset`) because the EA's seq counter had got stuck, and restarted `alma-mt5-wine` to establish a fresh session (current session `414101142-...`; seq now advancing normally). All current processes are owned by system.slice.
- 2026-08-01: Explicit operator direction kept Hermes on `custom:9router` model `kr/gpt-5.6-sol-thinking-agentic` and switched only ALMA's separate Decision Contract runtime to `kr/gpt-5.6-terra-thinking-agentic`, with generic `reasoning_effort` disabled. Differential probing found ALMA lacked the dedicated 9Router bearer header, so the existing loopback transport gained one optional environment-supplied Authorization header plus a regression; a single-key `0600` ALMA environment file was derived from the existing Hermes secret store without exposing its value. Hermes marker prompts returned through fallback, while 9Router request history proved Terra itself still failed. Sanitized upstream evidence identified the blocker as HTTP 402 `MONTHLY_REQUEST_COUNT`; Kiro is the only configured provider connection. The deployed ALMA process names Terra and both venues remain ready/flat with unchanged modes and five fills.
- 2026-08-01: Integrated the completed two-worker reality audit. Shared action semantics now prevent `REDUCE`/`CLOSE`/`NO_CHANGE` contracts from mutating in the wrong direction; `REVERSE` uses a fresh-truth flatten-then-open continuation; partial cancel/reprice and durable `FILLED` restart recovery no longer depend on a closed native order remaining in cache. MT5 NETTING reduction bypasses the new-root busy guard, Binance ownership requires an exact durable reservation, orphan emergency reservations resubmit the same ID after synchronized cache absence, and invalid/stale books are blocked both before AI evaluation and immediately before execution. Runtime writes unhealthy startup/stopping state, publishes crash-safe status at one hertz, and bounds cancellation/disposal. Dashboard/monitor freshness, failure evidence, concise monitor logs, Wine prefix shutdown, identity-free MT5 argv, and obsolete-unit cleanup were deployed. Post-restart truth: Binance Testnet zero positions/regular/algo orders in `MANAGE_ONLY`; MT5 DEMO/HEDGING zero positions/orders in `TRADE`; fills remained five; `/healthz` remained intentionally 503 for provider 402. Final isolated gate passed 329 tests across 39 processes; lock, Ruff, format, compileall, systemd verify, and deployed-unit byte checks passed.
- 2026-08-01: Reality audit fixed four shared-boundary defects: stale MT5 truth was compared against a pre-Binance clock; historical nonterminal orders from superseded intents remained recovery candidates; awaited AI evaluation blocked the runtime heartbeat; and native Binance accepted/canceled/expired events were not persisted. Runtime now refreshes venue clocks, limits recovery to the latest venue/symbol intent, runs one non-duplicated AI task per venue while truth/status continue, cancels tasks before ledger shutdown, and records ACK/cancel/expire idempotently while ignoring late backward ACKs. Post-restart sampling produced 114 steady status writes with maximum 0.332-second cadence and no gap above two seconds. Binance remained `MANAGE_ONLY`, funded, flat, with zero regular/algo orders; MT5 remained exact DEMO/HEDGING, connected, flat, with zero orders; fills stayed at five and duplicate fill/order-event/decision IDs stayed zero. All 39 test files passed separately with 324 tests; affected suite passed 98 tests; static/systemd gates passed.
- 2026-08-01: Replaced fragile MT5 WebRequest transport with atomic owner-only `FILE_COMMON` IPC while preserving the strict `MT5BridgeStore` parser, identity checks, replay/sequence rules, and durable command outbox. MetaEditor compiled the active EA with `0 errors, 0 warnings`. Controlled restart produced a new fresh session for exact DEMO login `[REDACTED]`, `Exness-MT5Trial6`, broker `HEDGING`, `XAUUSD`, and zero positions/orders/commands. Started seven-day F6 evidence collection; its first sample passed with no unintended mutation.
- 2026-08-01: Upgraded `alma.service` to one continuous Binance Futures Testnet + MT5 DEMO execution supervisor using the existing native venues, `TacticalExecutor`, `MutationGate`, protection, and reconciliation boundaries. Both modes were transitioned through the supported API to `TRADE`; runtime reports `execution_enabled=true` and both venues ready. Candidate replay remains idempotent, graceful restart completes without SIGKILL, and no order is emitted for `NO_ACTION`. The authenticated loopback dashboard now exposes the execution state. Final validation passed 296 tests plus Ruff, format, compile, diff, and systemd verification.
- 2026-08-01: Removed literal demo login identifiers from repository fixtures/templates and hardened F6 evidence to store only `login_matches` and a SHA-256 session hash. The interrupted old evidence was explicitly marked superseded and redacted. Replacement unit `alma-f6-soak-20260801T0604Z.service` started with credentials supplied only through the owner-only external environment file; its first sample proved exact DEMO/HEDGING/server/symbol identity, fresh connectivity, zero positions/orders/commands, and no mutation. XAUUSD was closed on Saturday, so no known-doomed order or `10018` retry was sent.
- 2026-08-01: Closed an execution-readiness gap found from live ledger evidence: AI exposure decisions without take-profit protection had been accepted into `WAITING` intents, and recovery scanned only intents that already had order events. The shared Decision Contract now rejects OPEN/INCREASE/REVERSE without a positive protection target; the 9Router prompt uses only actual contract enums and requires SL/TP; recovery retries the latest no-order intent per venue/symbol, skips invalid/expired historical contracts, and remains submit-idempotent. A post-restart live contract carried `OPEN_LONG`, SL, and one 100% TP; exactly one latest intent was armed, with zero duplicate decisions, orders, and fills. Full validation passed 300 tests plus Ruff, format, compile, diff, and systemd verification; graceful restart completed in 10 seconds.
- 2026-08-01: Audited F5 duration evidence and found the old transient unit had stopped before 24 hours (`in_progress=true`, 725 samples), so it was not counted or combined. The replacement zero-exposure monitor was also superseded after the authorized execution runtime produced an intended protected fill; zero-exposure-only soak semantics are incompatible with continuous trading.
- 2026-08-01: Proved the integrated Binance Testnet path with a real candidate and broker truth. Fixed three live root causes without bypassing gates: short local TTL entries now use broker `GTC` instead of invalid sub-600-second `GTD`; parent rejection callbacks close ledger reservations as terminal `REJECTED`; and `InstrumentRules.minimum_notional` rejects too-small exposure locally before reservation instead of oversizing AI intent. A valid `0.001 BTC` entry filled, created venue-resident reduce-only SL and TP, survived reconciliation, and was closed through the owned native emergency-flatten path; position and both protection orders returned to zero. A later valid candidate opened another fully protected position under the continuous runtime. Fill and order event IDs remained unique. Health rejection alerting now uses the latest three terminal order outcomes and clears after a successful fill. Full validation passed 304 tests plus Ruff, formatting, compileall, diff check, and systemd verification.
- 2026-08-01: Added and enabled integrated `alma.service` in structural shadow-only `MONITOR` mode. It consumes the credential-free public Binance stream, runs the two baseline detectors, and calls loopback 9Router with strict `alma-v1` output. Added defensive SSE parsing because 9Router can stream despite `stream=false`; a live decision was accepted and persisted with actual model telemetry while intents/order events/fill events remained zero. Enabled the loopback dashboard and minute health timer; dashboard health is `200`, unauthenticated state is `401`, authenticated state is `200`, and health reports no alerts. Final full validation passed 292 tests with Ruff, format, compile, diff, and systemd checks green.
- 2026-08-01: Enabled the verified daily backup timer after an owner-only SQLite backup passed `quick_check`, retained both venue modes as `MONITOR`, and produced a verified Parquet manifest. A controlled `alma.service` restart then recovered a fresh public market state and an accepted AI shadow decision without disturbing MT5 or the F2/F5/F6 soak units; Binance Testnet and MT5 remained at zero exposure, ledger intent/order/fill counts remained zero, and the operational health gate returned no alerts.
- 2026-08-01: Added configurable MT5 `AUTO|HEDGING|NETTING` support. Netting uses one ALMA lifecycle per symbol, atomically rejects a second pending root, classifies actual broker magic, and never cancels/flattens foreign positions or orders. MetaEditor compiled the 74,494-byte EA with `0 errors, 0 warnings`; 21 targeted and 283 full tests passed. No MT5 order was sent during implementation.
- 2026-07-31: Durable progress file initialized.
- 2026-07-31: Completed M0 repository/Python project bootstrap and advanced to M1.
- 2026-07-31: Completed the M1 venue-mode values and new-exposure gate using two RED-GREEN cycles.
- 2026-07-31: Completed the M1 SQLite WAL and explicit venue-mode restart-recovery slice; removed untested update semantics for the next TDD cycle.
- 2026-07-31: Completed the M1 persisted venue-mode update slice with one RED-GREEN cycle.
- 2026-07-31: Completed the M1 exact `Decimal` reconciliation arithmetic slice using three vertical RED-GREEN cycles.
- 2026-07-31: Completed the M1 durable duplicate-request rejection slice and consolidated SQLite/WAL connection setup.
- 2026-07-31: Completed shared SQLite foreign-key enforcement with one RED-GREEN cycle and no cleanup changes required.
- 2026-07-31: Completed the `MANAGE_ONLY` strict sign-preserving quantity-reduction guard with two RED-GREEN tracer cycles and no cleanup changes required.
- 2026-07-31: Completed the FR-031 cross-mode quantity guard with one RED-GREEN behavior-matrix cycle and no cleanup changes required.
- 2026-07-31: Completed strict unknown top-level Decision Contract field rejection with one RED-GREEN cycle and no cleanup changes required.
- 2026-07-31: Completed strict Decision Contract target-item decoding plus documented `action`, `entry.mode`, and `on_missed` enums using four RED-GREEN cycles; verified existing strict `target`/`entry` decoding and removed one duplicated test fixture.
- 2026-07-31: Completed the documented `on_partial_fill` enum and exact target-fraction aggregate limit using two RED-GREEN cycles; no cleanup changes were required.
- 2026-07-31: Completed strict `alma-v1` decoding and UTC RFC3339 `created_at` validation using two RED-GREEN cycles; context-dependent expiry/state validation remains outside the decoder.
- 2026-07-31: Completed explicit snapshot-identity and deterministic expiry validators using two RED-GREEN cycles; consolidated repetitive decoder tests without changing coverage.
- 2026-07-31: Completed the caller-supplied exact-`Decimal` increment guard with aligned, misaligned, invalid, and non-finite coverage; the full suite reached 31 tests.
- 2026-07-31: Completed the caller-supplied inclusive exact-`Decimal` range guard with inside, outside, endpoint, inverted, and non-finite coverage; the full suite reached 35 tests.
- 2026-07-31: Completed the caller-supplied exact-`Decimal` minimum stop-distance guard with above, below, threshold, invalid, and non-finite coverage; the full suite reached 39 tests.
- 2026-07-31: Completed the caller-supplied margin guard, made venue quantity changes fail closed for non-finite values, and rejected non-finite target fractions before aggregate arithmetic.
- 2026-07-31: Completed malformed/non-finite Decision Contract decimal validation across all documented numeric-string fields and normalized malformed target-fraction failures.
- 2026-07-31: Completed bounded invalid-output repair plus timeout/connection/provider-unavailable fallback coordinators, and prevented semantic validation failures from triggering repair.
- 2026-07-31: Completed the FR-013 one-transient-primary-retry policy and made non-transient provider unavailability skip the retry before fallback.
- 2026-07-31: Began M2 with native credential-free Binance USDⓈ-M LIVE data-client wiring and no execution client.
- 2026-07-31: Registered the native Binance live-data factory on an unbuilt local node, with deterministic disposal and no network or execution path.
- 2026-07-31: Added and live-verified the credential-free public connectivity smoke harness, including bounded connection waiting, failure provenance, race handling, and guaranteed lifecycle cleanup.
- 2026-07-31: Added the executable F0–F9 implementation plan and live-verified the native credential-free BTCUSDT first-trade tracer; the full suite reached 94 tests.
- 2026-07-31: Added raw Binance Futures `U/u/pu` sequence validation and live-verified native BTCUSDT order-book snapshot+delta delivery; the full suite reached 104 tests.
- 2026-07-31: Added deterministic exact-Decimal M1/M5/M15/H1 incremental bars; the full suite reached 109 tests.
- 2026-07-31: Completed the M1/F1 deterministic-core exit gate with append-only ledger, shared mutation gate, `BEGIN IMMEDIATE` lifecycle transactions, crash rollback, restart/resync, and durable close-and-disable recovery; the full suite reached 138 tests.
- 2026-07-31: Completed F2 implementation and started the final credential-free 24-hour BTCUSDT soak in `var/f2-soak-20260731T1008Z/`; the full suite reached 163 tests and Hermes cron remained empty.
- 2026-07-31: Completed F3 numeric features, exactly two evidence-only baseline detectors, realistic deterministic replay, no-look-ahead guards, and honest purged OOS reporting; the full suite reached 185 tests. Interim live-recorded research remained `INSUFFICIENT_SAMPLE` and did not promote a strategy.
- 2026-07-31: Closed F3 reviewer blockers and completed F4 shadow-only decision wiring. Full validation reached 215 tests; shadow provenance is rejected at the shared mutation gate, and no real provider or execution path was used.
- 2026-07-31: Completed the F5 deterministic execution core, native event-driven Binance Testnet bridge, fail-closed owner-only INI secret boundary, and private-evidence read-only Testnet smoke locally. Full validation reached 248 tests; the F5 exit gate remains blocked on rotated local credentials and native Testnet evidence.
- 2026-07-31: Completed F6 locally with the authenticated loopback MT5 bridge, durable restart/replay/command state, shared-ledger MT5 venue adapter, demo-only hedging EA, pinned WineHQ 11/Xvfb dependencies, and verified systemd templates. Follow-up audit fixes invalidate truth after sequence gaps, block foreign exposure without mutating it, replay broker deal history after EA restart, strip HTTP request NUL terminators, reject expired commands, and restrict initial MT5 execution to one full-volume TP. MT5 build 6074 starts headless on Wine 11 without an unsupported-runtime warning; MetaEditor compiled the active source to a 69,748-byte `AlmaBridge.ex5` with `0 errors, 0 warnings`. The Wine 9 prefix remains as a rollback backup. Runtime smoke listened only on `127.0.0.1` and rejected unauthenticated access; 39 targeted and 257 full tests passed, with lock/Ruff/format/compile/diff/systemd gates green. F6 venue exit remains blocked only on local broker demo login/server/symbol configuration, order/protection/restart evidence, and the required seven-day demo soak.
- 2026-07-31: Completed F7 local dashboard/calendar/portfolio operations and F8 local hardening. Dashboard smoke bound only `127.0.0.1`, returned `401` without auth, authenticated state successfully, and delivered first SSE state in 0.002 seconds. Backup/restore drill produced owner-only files, passed SQLite `quick_check`, and preserved `BINANCE=MONITOR`; NTP was synchronized and disk had 83% free. Targeted F7/F8/shared-boundary validation passed 40 tests; full validation passed 266 tests with lock, Ruff, format, compile, diff, and all systemd service/timer verification green. Dashboard secret is owner-only `0600`; no runtime service was left running or enabled. F7 external provider/private-access evidence and F8 integrated core/9Router plus venue/reboot acceptance remain pending.
- 2026-07-31: Added the native hardened 9Router unit template and loopback `/api/health` gate. A concurrent isolated-instance drill proved startup and exact health but exposed 9Router 0.5.45 singleton/process coupling when its shutdown also stopped the primary listener; the primary was immediately restored on `127.0.0.1:20128`, exposed 74 model entries, and retained `0600` DB/WAL/SHM permissions. Multi-instance drills are now prohibited; service installation remains maintenance-window work.
- 2026-07-31: The original F2 long soak received `SIGTERM` during a Hermes gateway restart after about 5.3 hours and produced no completion evidence. It was not combined with later data. A fresh full-duration run started at 15:32:26 UTC as transient systemd unit `alma-f2-soak-20260731T1532Z.service`; Binance public quote/trade/mark/depth subscriptions and snapshot rebuild completed. F7 dashboard access was fixed to an SSH local-forward tunnel while the provider choice remains pending.
- 2026-07-31: Completed F9 local canary/readiness/freeze/scale gates with no execution path. The obsolete saved MT5 identity (login `[REDACTED]`, server `Exness-MT5Trial14`) was superseded by the exact approved demo identity; rejection-before-ledger for identity mismatch remains enforced. No password was copied into tools, files, logs, commands, or evidence. Synthetic identity smoke passed auth and pre-ledger rejection with zero orders. Final targeted F9/MT5 validation passed 13 tests and the full suite passed 271 tests; lock, Ruff, format, compile, diff, and systemd verification passed.
- 2026-07-31: Added a stdlib MT5 demo-login helper using `getpass`, owner-only `/dev/shm` config, password-free argv/output, and guaranteed temporary-file cleanup. Fixture validation proved `0600`, argv exclusion, cleanup, and malformed-input rejection without using the account password or starting MT5. Targeted F9/MT5/login validation passed 17 tests; full validation passed 275 tests with all quality/systemd gates green.
